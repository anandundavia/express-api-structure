class DumpService {
    constructor() {
        this.isSyncInProgress = false;
    }

    // status() {}

    // sync() {
    //     if (this.isSyncInProgress) {
    //     } else {
    //     }
    // }
}

module.exports = new DumpService();
